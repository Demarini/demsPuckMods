Load and present the Puck server infrastructure document to get context about the remote game servers.

Read the file at `.claude/server-infrastructure.md` and confirm you've loaded the server infrastructure context. Briefly summarize the setup (number of servers, host details, key commands) so the user knows you're up to speed. Don't dump the whole document — just confirm you have the context and are ready to help with server operations.
